import { Module } from '@nestjs/common';

/**
 * Phase 2 stub — Public memorial/tribute page.
 * Schema model `MemorialPage` already exists (Phase 4).
 * Implementation deferred to Phase 2 feature work.
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class MemorialModule {}
