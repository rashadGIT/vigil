import { Module } from '@nestjs/common';

/**
 * Phase 2 stub — Decedent chain-of-custody tracking.
 * Schema model `DecedentTracking` already exists (Phase 4).
 * Implementation deferred to Phase 2 feature work.
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class TrackingModule {}
